# ✅ Финальная настройка - Используем GitHub Pages

## 🎯 Используем работающую ссылку

**Web App URL**: `https://donikhodjaev.github.io/misbaha/`

Эта ссылка уже работает и настроена правильно!

---

## 🤖 Настройка Telegram Mini App

### Шаг 1: Откройте BotFather
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newapp` или `/myapps`
3. Выберите вашего бота

### Шаг 2: Настройте Mini App
Заполните:
- **Title**: `Misbaha - Счетчик Зикра`
- **Description**: `Современное приложение для подсчета зикра`
- **Photo**: Загрузите `assets/ico.png`
- **Short name**: `misbaha`
- **Web App URL**: `https://donikhodjaev.github.io/misbaha/`

**Важно:** URL должен заканчиваться на `/`!

### Шаг 3: Готово!
Теперь в боте будет кнопка для открытия приложения.

---

## 🖥️ Запуск бота на сервере

### Подключитесь к серверу:
```bash
ssh root@83.147.247.11
```

### На сервере выполните:

```bash
# 1. Создайте директорию
mkdir -p /root/misbaha
cd /root/misbaha

# 2. Создайте файл telegram-bot.js
# Скопируйте файл с вашего компьютера через SCP:
# (на вашем компьютере)
# scp telegram-bot.js root@83.147.247.11:/root/misbaha/

# Или создайте вручную через nano:
nano telegram-bot.js
# Вставьте содержимое файла, сохраните (Ctrl+O, Enter, Ctrl+X)

# 3. Установите Node.js (если не установлен)
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# 4. Установите зависимости
npm install node-telegram-bot-api

# 5. Создайте systemd сервис
nano /etc/systemd/system/misbaha-bot.service
```

Вставьте в файл:
```ini
[Unit]
Description=Misbaha Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/misbaha
Environment="TELEGRAM_BOT_TOKEN=8519726866:AAFD94FpjP1ToMce_ejIK8Y2IscIKocqFj0"
Environment="WEB_APP_URL=https://donikhodjaev.github.io/misbaha/"
ExecStart=/usr/bin/node /root/misbaha/telegram-bot.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

```bash
# 6. Запустите бота
systemctl daemon-reload
systemctl enable misbaha-bot
systemctl start misbaha-bot

# 7. Проверьте статус
systemctl status misbaha-bot

# 8. Посмотрите логи
journalctl -u misbaha-bot -f
```

---

## ✅ Проверка работы

1. **Проверьте бота в Telegram:**
   - Найдите вашего бота
   - Отправьте `/start`
   - Нажмите кнопку "📿 Открыть счетчик зикра"
   - Приложение должно открыться!

2. **Проверьте команды:**
   - `/help` - справка
   - `/stats` - статистика (после использования приложения)

3. **Проверьте логи на сервере:**
   ```bash
   journalctl -u misbaha-bot -n 50
   ```

---

## 🔄 Обновление приложения

Если вы обновили приложение на GitHub Pages:

1. Сделайте изменения в коде
2. Соберите: `npm run build:web`
3. Загрузите папку `dist` в репозиторий GitHub
4. Приложение автоматически обновится на GitHub Pages

Бот продолжит работать с той же ссылкой!

---

## 📝 Полезные команды

### На сервере:
```bash
# Статус бота
systemctl status misbaha-bot

# Запуск
systemctl start misbaha-bot

# Остановка
systemctl stop misbaha-bot

# Перезапуск
systemctl restart misbaha-bot

# Логи
journalctl -u misbaha-bot -f
```

---

## 🎉 Готово!

Теперь у вас:
- ✅ Приложение работает на GitHub Pages
- ✅ Telegram Mini App настроен
- ✅ Бот работает на сервере
- ✅ Все функции интегрированы

**Наслаждайтесь!** 🚀

