# 🚀 Деплой на Netlify - Пошаговая инструкция

## 📋 Подготовка

### 1. Соберите приложение для Netlify:
```bash
npm run build:netlify
```

**Важно:** Используйте `build:netlify` для правильных путей на Netlify!

Убедитесь, что папка `dist` создалась и содержит файлы.

## 🌐 Способ 1: Drag & Drop (Самый простой)

### Шаг 1: Соберите проект для Netlify
```bash
npm run build:netlify
```

**Важно:** Используйте `build:netlify` для правильных путей!

### Шаг 2: Загрузите на Netlify
1. Откройте https://app.netlify.com
2. Войдите или зарегистрируйтесь
3. На главной странице найдите область **"Want to deploy a new site without connecting to Git? Drag and drop your site output folder here"**
4. Перетащите папку `dist` в эту область
5. Дождитесь загрузки (обычно 1-2 минуты)

### Шаг 3: Получите ссылку
- Netlify автоматически создаст ссылку вида: `https://random-name-123.netlify.app`
- Эта ссылка будет вашим Web App URL для Telegram Mini App

### Шаг 4: Переименуйте сайт (опционально)
1. В настройках сайта → **Site settings** → **Change site name**
2. Выберите более понятное имя (например: `misbaha-app`)

## 🔄 Способ 2: Подключение GitHub (Автоматический деплой)

### Шаг 1: Подготовьте репозиторий
```bash
git add .
git commit -m "Готово к деплою на Netlify"
git push
```

### Шаг 2: Подключите к Netlify
1. Откройте https://app.netlify.com
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и авторизуйтесь
4. Выберите репозиторий `bi_test` (или ваш репозиторий)

### Шаг 3: Настройте сборку
- **Build command**: `npm run build:netlify`
- **Publish directory**: `dist`
- **Base directory**: (оставьте пустым)

### Шаг 4: Нажмите "Deploy site"

### Шаг 5: Настройте переменные окружения (если нужно)
В настройках сайта → **Build & deploy** → **Environment variables**:
- Можно добавить переменные, если они нужны для сборки

## ✅ Проверка деплоя

1. Откройте ссылку Netlify в браузере
2. Убедитесь, что приложение загружается
3. Проверьте работу всех функций

## 🔗 Обновление приложения

### Если используете Drag & Drop:
1. Соберите проект: `npm run build:netlify`
2. Перетащите новую папку `dist` на Netlify (заменит старую)

### Если используете GitHub:
1. Внесите изменения
2. Сделайте commit и push:
```bash
git add .
git commit -m "Обновление приложения"
git push
```
3. Netlify автоматически пересоберет и обновит сайт!

## 📱 Настройка для Telegram Mini App

После получения ссылки Netlify:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp` или `/myapps`
3. Выберите вашего бота
4. Укажите:
   - **Web App URL**: `https://ваш-сайт.netlify.app`
   - **Short name**: `misbaha`
   - **Title**: `Misbaha - Счетчик Зикра`
   - **Description**: `Современное приложение для подсчета зикра`
   - **Photo**: Загрузите `assets/ico.png`

## 🔧 Настройка домена (опционально)

1. В настройках сайта → **Domain settings**
2. Нажмите **"Add custom domain"**
3. Введите ваш домен или используйте бесплатный поддомен Netlify

## ⚠️ Важные замечания

1. **HTTPS**: Netlify автоматически предоставляет HTTPS сертификат
2. **Кэширование**: Netlify автоматически кэширует статические файлы
3. **Ограничения**: Бесплатный план Netlify имеет лимиты, но их достаточно для большинства приложений
4. **Обновление**: После каждого push в GitHub (если подключен) сайт автоматически обновится

## 🎉 Готово!

Ваше приложение теперь доступно по адресу Netlify и готово для использования в Telegram Mini App!

