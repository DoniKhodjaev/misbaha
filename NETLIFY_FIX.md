# 🔧 Исправление путей для Netlify

## Проблема

Приложение настроено для GitHub Pages с префиксом `/misbaha/`, но на Netlify нужны корневые пути.

## ✅ Решение

Создана отдельная сборка для Netlify без префикса `/misbaha/`.

## 🚀 Использование

### Сборка для Netlify:

```bash
npm run build:netlify
```

Эта команда:
1. Временно заменяет `app.json` на `app.netlify.json` (с корневыми путями)
2. Собирает приложение
3. Исправляет пути в файлах (убирает `/misbaha/`)
4. Восстанавливает оригинальный `app.json`

### Загрузка на Netlify:

1. Выполните сборку:
```bash
npm run build:netlify
```

2. Перетащите папку `dist` на https://app.netlify.com

3. Готово! Приложение будет работать на Netlify.

## 📝 Что изменилось

- ✅ Создан `app.netlify.json` - конфигурация для Netlify (корневые пути)
- ✅ Создан `scripts/fix-paths-netlify.js` - скрипт для исправления путей
- ✅ Создан `web/manifest.netlify.json` - манифест для Netlify
- ✅ Добавлена команда `build:netlify` в package.json

## 🔄 Две версии сборки

- **`npm run build:web`** - для GitHub Pages (с `/misbaha/`)
- **`npm run build:netlify`** - для Netlify (корневые пути)

## ⚠️ Важно

После сборки для Netlify, если нужно собрать для GitHub Pages, просто выполните:
```bash
npm run build:web
```

Оригинальный `app.json` автоматически восстанавливается.

