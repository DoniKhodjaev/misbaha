# Telegram Mini App - Руководство по интеграции

## 🎯 Что такое Telegram Mini App?

Telegram Mini App (ранее Web App) - это веб-приложение, которое открывается внутри Telegram через специальную кнопку в боте. Ваше приложение **MISBAHA** идеально подходит для этого, так как уже имеет веб-версию!

## ✅ Преимущества Telegram Mini App

- ✅ **Не требует установки** - открывается прямо в Telegram
- ✅ **Все функции работают** - счетчик, статистика, настройки
- ✅ **Быстрый доступ** - одна кнопка в боте
- ✅ **Работает на всех устройствах** - iOS, Android, Desktop
- ✅ **Бесплатно** - не требует публикации в App Store/Google Play

## 📋 Что нужно для запуска

1. **Telegram Bot** - создайте бота через [@BotFather](https://t.me/BotFather)
2. **Веб-хостинг** - разместите приложение на сервере (Netlify, Vercel, GitHub Pages)
3. **HTTPS** - обязательно нужен SSL сертификат (бесплатно на Netlify/Vercel)

## 🚀 Пошаговая инструкция

### Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Misbaha Counter")
   - Введите username бота (например: `misbaha_counter_bot`)
4. Сохраните **токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Сборка веб-версии приложения

```bash
# Соберите приложение для веба
npm run build:web
```

Это создаст папку `dist` с готовым приложением.

### Шаг 3: Размещение на хостинге

#### Вариант A: Netlify (рекомендуется, самый простой)

1. Зайдите на https://netlify.com
2. Зарегистрируйтесь (бесплатно)
3. Перетащите папку `dist` на страницу Netlify
4. Получите URL: `https://ваш-проект.netlify.app`
5. **Важно:** Убедитесь, что URL начинается с `https://`

#### Вариант B: Vercel

```bash
npm i -g vercel
npm run build:web
cd dist
vercel
```

#### Вариант C: GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите содержимое папки `dist` в репозиторий
3. Включите GitHub Pages в настройках
4. URL будет: `https://ваш-username.github.io/репозиторий`

### Шаг 4: Настройка бота с кнопкой Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота из списка
4. Введите:
   - **Title:** Misbaha - Счетчик Зикра
   - **Description:** Современное приложение для подсчета зикра
   - **Photo:** Загрузите иконку (файл `assets/ico.png`)
   - **Short name:** misbaha (только латиница, без пробелов)
   - **Web App URL:** `https://ваш-проект.netlify.app` (URL из шага 3)

### Шаг 5: Создание кнопки в боте

Создайте файл `bot.js` для управления ботом:

```javascript
const TelegramBot = require('node-telegram-bot-api');

const token = 'ВАШ_ТОКЕН_БОТА';
const bot = new TelegramBot(token, { polling: true });

// Команда /start - показывает кнопку для открытия Mini App
bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id;
  
  bot.sendMessage(chatId, '👋 Добро пожаловать в Misbaha!', {
    reply_markup: {
      inline_keyboard: [[
        {
          text: '📿 Открыть счетчик зикра',
          web_app: { url: 'https://ваш-проект.netlify.app' }
        }
      ]]
    }
  });
});

// Команда /help
bot.onText(/\/help/, (msg) => {
  const chatId = msg.chat.id;
  bot.sendMessage(chatId, 
    '📿 Misbaha - Счетчик Зикра\n\n' +
    'Используйте кнопку ниже, чтобы открыть приложение.\n\n' +
    'Команды:\n' +
    '/start - Открыть приложение\n' +
    '/help - Показать эту справку'
  );
});

console.log('Бот запущен!');
```

### Шаг 6: Запуск бота

```bash
# Установите зависимости
npm install node-telegram-bot-api

# Запустите бота
node bot.js
```

Или используйте сервис для постоянной работы (Heroku, Railway, Render).

## 🎨 Дополнительные возможности

### Интеграция с Telegram Web App API

Telegram предоставляет API для интеграции с Mini App. Вы можете:

1. **Получать данные пользователя:**
   - Имя, username, фото
   - Язык интерфейса Telegram
   - Тема (светлая/темная)

2. **Взаимодействие с ботом:**
   - Отправка сообщений из приложения
   - Закрытие Mini App программно

3. **Визуальные эффекты:**
   - Изменение цвета кнопки
   - Вибрация (на мобильных)
   - Показ всплывающих окон

### Пример интеграции Telegram Web App API

Добавьте в начало `App.js`:

```javascript
// Проверка, запущено ли в Telegram
const isTelegram = typeof window !== 'undefined' && window.Telegram?.WebApp;

if (isTelegram) {
  const tg = window.Telegram.WebApp;
  
  // Инициализация
  tg.ready();
  tg.expand();
  
  // Установка цвета темы
  tg.setHeaderColor('#004734');
  tg.setBackgroundColor('#004734');
  
  // Получение данных пользователя
  const user = tg.initDataUnsafe?.user;
  console.log('Telegram user:', user);
}
```

## 📱 Тестирование

1. Найдите вашего бота в Telegram (по username)
2. Отправьте команду `/start`
3. Нажмите на кнопку "📿 Открыть счетчик зикра"
4. Приложение откроется в полноэкранном режиме внутри Telegram

## 🔧 Настройка для Telegram

### Адаптация под Telegram WebView

Приложение уже адаптировано для веба, но можно улучшить:

1. **Скрыть адресную строку** (уже работает в Mini App)
2. **Оптимизировать для маленьких экранов** (уже сделано)
3. **Использовать Telegram цветовую схему** (опционально)

### Файл для Telegram-специфичных настроек

Создайте файл `telegram-config.js`:

```javascript
// telegram-config.js
export const getTelegramConfig = () => {
  if (typeof window === 'undefined' || !window.Telegram?.WebApp) {
    return null;
  }
  
  const tg = window.Telegram.WebApp;
  
  return {
    // Данные пользователя
    user: tg.initDataUnsafe?.user,
    
    // Тема Telegram
    themeParams: tg.themeParams,
    
    // Платформа
    platform: tg.platform,
    
    // Версия
    version: tg.version,
    
    // Методы
    close: () => tg.close(),
    expand: () => tg.expand(),
    ready: () => tg.ready(),
    setHeaderColor: (color) => tg.setHeaderColor(color),
    setBackgroundColor: (color) => tg.setBackgroundColor(color),
    showAlert: (message) => tg.showAlert(message),
    showConfirm: (message, callback) => tg.showConfirm(message, callback),
    vibrate: (pattern) => tg.HapticFeedback?.impactOccurred?.(pattern),
  };
};
```

## 🚨 Важные замечания

1. **HTTPS обязателен** - Telegram требует безопасное соединение
2. **CORS настройки** - убедитесь, что сервер разрешает запросы из Telegram
3. **Размер приложения** - старайтесь минимизировать размер (Telegram имеет лимиты)
4. **Тестирование** - тестируйте на реальных устройствах, не только в браузере

## 📚 Полезные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web App API](https://core.telegram.org/bots/webapps)
- [Примеры Mini Apps](https://github.com/telegram-mini-apps)
- [Node Telegram Bot API](https://github.com/yagop/node-telegram-bot-api)

## 🎉 Готово!

Теперь ваше приложение доступно как Telegram Mini App! Пользователи могут открыть его одной кнопкой прямо из Telegram.

## 💡 Дополнительные идеи

1. **Синхронизация данных** - сохранять данные пользователя в облаке
2. **Социальные функции** - делиться достижениями через бота
3. **Напоминания** - отправлять напоминания о зикре через бота
4. **Статистика** - отправлять ежедневную статистику через бота

