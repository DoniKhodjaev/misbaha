# 🚀 Полная инструкция по деплою

## 📋 План действий

1. ✅ Собрать приложение
2. ✅ Загрузить на Netlify
3. ✅ Настроить Telegram Mini App
4. ✅ Запустить бота на сервере

---

## 🌐 Часть 1: Деплой на Netlify

### Шаг 1: Сборка приложения

На вашем компьютере:
```bash
npm run build:netlify
```

**Важно:** Используйте `build:netlify` для правильных путей на Netlify!

Проверьте, что папка `dist` создалась.

### Шаг 2: Загрузка на Netlify

**Вариант A: Drag & Drop (Самый простой)**

1. Откройте https://app.netlify.com
2. Войдите или зарегистрируйтесь
3. Перетащите папку `dist` в область "Drag and drop your site output folder here"
4. Дождитесь загрузки (1-2 минуты)
5. Скопируйте ссылку (например: `https://misbaha-app-123.netlify.app`)

**Вариант B: Через GitHub (Автоматический деплой)**

1. На Netlify: **Add new site** → **Import an existing project**
2. Выберите GitHub и ваш репозиторий
3. Настройки:
   - **Build command**: `npm run build:web`
   - **Publish directory**: `dist`
4. Нажмите **Deploy site**

### Шаг 3: Проверка

Откройте ссылку Netlify в браузере и убедитесь, что приложение работает.

**Ваш Web App URL**: `https://ваш-сайт.netlify.app` (запомните его!)

---

## 🤖 Часть 2: Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp` или `/myapps`
3. Выберите вашего бота
4. Заполните:
   - **Title**: `Misbaha - Счетчик Зикра`
   - **Description**: `Современное приложение для подсчета зикра`
   - **Photo**: Загрузите `assets/ico.png`
   - **Short name**: `misbaha`
   - **Web App URL**: `https://donikhodjaev.github.io/misbaha/`

---

## 🖥️ Часть 3: Запуск бота на сервере

### Шаг 1: Подключение к серверу

```bash
ssh root@83.147.247.11
```

Введите root пароль.

### Шаг 2: Подготовка сервера

```bash
# Проверка Node.js
node --version

# Если не установлен:
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
```

### Шаг 3: Загрузка файлов

**Способ 1: Через SCP (с вашего компьютера)**

```bash
# На вашем компьютере
cd d:\Projects\bi_test
scp telegram-bot.js package.json root@83.147.247.11:/root/misbaha/
```

**Способ 2: Создать файлы на сервере**

```bash
# На сервере
mkdir -p /root/misbaha
cd /root/misbaha
nano telegram-bot.js
# Вставьте содержимое файла, сохраните (Ctrl+O, Enter, Ctrl+X)
```

### Шаг 4: Установка зависимостей

```bash
cd /root/misbaha
npm install node-telegram-bot-api
```

### Шаг 5: Настройка переменных

```bash
# Создайте файл .env или экспортируйте переменные
export TELEGRAM_BOT_TOKEN="8519726866:AAFD94FpjP1ToMce_ejIK8Y2IscIKocqFj0"
export WEB_APP_URL="https://ваш-сайт.netlify.app"
```

### Шаг 6: Создание systemd сервиса

```bash
nano /etc/systemd/system/misbaha-bot.service
```

Вставьте:
```ini
[Unit]
Description=Misbaha Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/misbaha
Environment="TELEGRAM_BOT_TOKEN=8519726866:AAFD94FpjP1ToMce_ejIK8Y2IscIKocqFj0"
Environment="WEB_APP_URL=https://donikhodjaev.github.io/misbaha/"
ExecStart=/usr/bin/node /root/misbaha/telegram-bot.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

### Шаг 7: Запуск бота

```bash
# Перезагрузить systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable misbaha-bot

# Запустить
systemctl start misbaha-bot

# Проверить статус
systemctl status misbaha-bot

# Посмотреть логи
journalctl -u misbaha-bot -f
```

---

## ✅ Проверка работы

1. **Проверьте бота в Telegram:**
   - Найдите вашего бота
   - Отправьте `/start`
   - Нажмите кнопку "📿 Открыть счетчик зикра"
   - Приложение должно открыться!

2. **Проверьте команды:**
   - `/help` - справка
   - `/stats` - статистика (после использования приложения)

3. **Проверьте логи на сервере:**
   ```bash
   journalctl -u misbaha-bot -n 50
   ```

---

## 🔄 Обновление

### Обновление приложения (GitHub Pages):
- Соберите: `npm run build:web`
- Загрузите папку `dist` в репозиторий GitHub
- Приложение автоматически обновится

### Обновление бота (сервер):
```bash
# Загрузите новый файл telegram-bot.js
# Перезапустите сервис
systemctl restart misbaha-bot
```

---

## 🐛 Решение проблем

### Бот не отвечает:
1. Проверьте логи: `journalctl -u misbaha-bot -f`
2. Проверьте токен в переменных окружения
3. Проверьте, что бот запущен: `systemctl status misbaha-bot`

### Приложение не открывается:
1. Проверьте URL в BotFather: должен быть `https://donikhodjaev.github.io/misbaha/`
2. Убедитесь, что сайт доступен: откройте https://donikhodjaev.github.io/misbaha/ в браузере
3. Проверьте, что URL заканчивается на `/`

### Ошибки на сервере:
1. Проверьте версию Node.js: `node --version` (должна быть 18+)
2. Проверьте установку зависимостей: `npm list node-telegram-bot-api`
3. Проверьте права доступа: `ls -la /root/misbaha`

---

## 📝 Полезные команды

### На сервере:
```bash
# Статус бота
systemctl status misbaha-bot

# Запуск
systemctl start misbaha-bot

# Остановка
systemctl stop misbaha-bot

# Перезапуск
systemctl restart misbaha-bot

# Логи
journalctl -u misbaha-bot -f
```

---

## 🎉 Готово!

Ваше приложение теперь:
- ✅ Работает на GitHub Pages
- ✅ Настроено как Telegram Mini App
- ✅ Бот работает на сервере
- ✅ Автоматически запускается при перезагрузке

**Наслаждайтесь!** 🚀

